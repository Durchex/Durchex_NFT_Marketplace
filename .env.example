# Deployment Environment Variables
# Copy this file to .env and fill in your values

# Private Key for Contract Deployment (REQUIRED)
# This should be the private key of the account that will deploy contracts
# NEVER commit this to version control - keep it secure!
PRIVATE_KEY=your_private_key_here_without_0x_prefix

# RPC URLs for different networks
ETHEREUM_RPC_URL=https://rpc.ankr.com/eth
POLYGON_RPC_URL=https://rpc.ankr.com/polygon
BSC_RPC_URL=https://rpc.ankr.com/bsc
ARBITRUM_RPC_URL=https://rpc.ankr.com/arbitrum
BASE_RPC_URL=https://mainnet.base.org
OPTIMISM_RPC_URL=https://rpc.ankr.com/optimism
AVALANCHE_RPC_URL=https://rpc.ankr.com/avalanche
HYPERLIQUID_RPC_URL=https://rpc.hyperliquid.xyz/evm

# Etherscan API Keys for contract verification (optional)
ETHERSCAN_API_KEY=your_etherscan_api_key
POLYGONSCAN_API_KEY=your_polygonscan_api_key
BSCSCAN_API_KEY=your_bscscan_api_key
ARBISCAN_API_KEY=your_arbiscan_api_key
BASESCAN_API_KEY=your_basescan_api_key
OPTIMISM_API_KEY=your_optimism_api_key
SNOWTRACE_API_KEY=your_snowtrace_api_key

# IPFS Configuration (REQUIRED for file uploads)
# Get these from https://app.pinata.cloud/
# 1. Sign up for a free account at pinata.cloud
# 2. Go to API Keys section
# 3. Create a new API key with pinFileToIPFS and pinJSONToIPFS permissions
# 4. Copy the API Key and API Secret
VITE_PINATA_API_KEY=your_pinata_api_key_here
VITE_PINATA_SECRET_KEY=your_pinata_secret_key_here

# Alternative: Use JWT token (recommended for client-side uploads)
# Create a JWT token in Pinata dashboard under API Keys
VITE_PINATA_JWT=your_pinata_jwt_token_here
TEZOS_NETWORK=mainnet

# SIU Network (when available)
VITE_SIU_CHAIN_ID=9999
VITE_SIU_RPC_URL=https://rpc.siu.network
VITE_SIU_BLOCK_EXPLORER=https://explorer.siu.network

# Monarch Network (when available)
VITE_MONARCH_CHAIN_ID=7777
VITE_MONARCH_RPC_URL=https://rpc.monarch.network
VITE_MONARCH_BLOCK_EXPLORER=https://explorer.monarch.network

# Backend API and Socket (production)
# Set these so the frontend talks to your backend. If unset, production uses same host as the page.
# VITE_API_BASE_URL=https://api.durchex.com/api/v1
# VITE_SOCKET_URL=https://api.durchex.com
# For WebSocket to work, your server/proxy must support WebSocket upgrade (e.g. nginx proxy_http_version 1.1; proxy_set_header Upgrade $http_upgrade; proxy_set_header Connection "upgrade";).

# LazyMint NFT contract (required for Buy & Mint of lazy-minted NFTs)
# You do NOT get this from "npx hardhat compile". You get it by DEPLOYING:
#   npx hardhat run scripts/deploy-lazy-mint-only.cjs --network polygon
#   (or --network sepolia / ethereum / etc.)
# Then copy the printed address below and rebuild the frontend.
VITE_APP_LAZY_MINT_CONTRACT_ADDRESS=0xYourLazyMintNFTContractAddress
# Optional: per-network overrides
# VITE_APP_LAZY_MINT_CONTRACT_ADDRESS_POLYGON=0x...
# VITE_APP_LAZY_MINT_CONTRACT_ADDRESS_ETHEREUM=0x...